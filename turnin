clear
git add --all
git commit -m "turning in"
pushcommit=$(git push origin master)
if [[ $? != 0 ]]; then
  echo -e "\n\n******** \n\n   Turn in failed!\n \n******** \n\n"
else
  echo -e "\n\n ----- \n\b\n Successfully Turned in!\n\n ------ \n\n"
fi
